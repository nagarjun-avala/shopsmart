export interface DeviceSession {
    id: string;
    deviceId: string;
    userAgent: string;
    platform: string;
    browser: string;
    ipAddress: string;
    createdAt: string;
    lastUsed: string;
}